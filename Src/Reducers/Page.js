import * as actionType from'../Const/Actions'
import pageState from '../Store/Pages';

const page = (state = pageState, action) => {
    switch (action.type) {
        
       
        default :
            return state;
    }
}

export default page;